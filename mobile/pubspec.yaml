name: sumobee
description: SumoBee - Knowledge Capture MVP
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  flutter_sharing_intent: ^1.0.0
  flutter_markdown: ^0.6.18
  url_launcher: ^6.2.0
  provider: ^6.1.1
  http: ^1.2.0
  intl: ^0.19.0
  shared_preferences: ^2.3.3
  google_fonts: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
